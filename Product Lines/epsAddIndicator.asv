% return the epsAddIndicator
% 
function [ indicator ] = epsAddIndicator( f_A,f_B,objectiveDimension )
indicator = f_A(0) - f_B(0);
for i = 2:objectiveDimension
    temp = f_A(i) - f_B(i);
    if(temp > indicator)
        indicator = temp;
    end
end
end

