%Sorting the population
%Based on DEB et al.: A FAST AND ELITIST MULTIOBJECTIVE GA: NSGA-II

function [rank] = fast-non-dominated-sort( parent,f )
   len = length(parent);
   n = zeros(1,len);
   for m = 1:len
       S=[];
       
   end
end

